using System.Reflection;

namespace Application;

public static partial class AssemblyReference
{
    public static Assembly ApplicationAssemblyReference = Assembly.GetAssembly(typeof(AssemblyReference));
}
